# 台股全能 AI 決策系統 MVP(taiwan_stock_ai/)所需套件。
# 對應報告技術選型:DuckDB 事實表、規則引擎訊號、LINE Bot(FastAPI)、0 token。

requests>=2.31
duckdb>=1.0
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# 前台/後台儀表板(dashboard/),同時給 DecisionRecord 平台(SQLite)用。
# altair/pandas 其實是 streamlit 的既有依賴,但 dashboard/app.py 直接
# import 它們畫 K 線圖,所以明確列出來,不要依賴 streamlit 的間接依賴版本。
streamlit>=1.38
altair>=5.0
pandas>=2.0

# 測試
pytest>=8.0
