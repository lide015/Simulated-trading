# 複製成 .streamlit/secrets.toml(本機測試 Streamlit Cloud 部署行為時用),
# 或直接在 Streamlit Community Cloud 的 App settings → Secrets 貼同樣內容。
# secrets.toml 本身不會被 git 追蹤(見 .gitignore),只有這份 .example 會。

DASHBOARD_PASSWORD = "換成一組夠長的密碼,不要用這個範例值"
